#include "main.h"


/**
 * mod- a function to get the modular differences between two numbers
 * @a: first number
 * @b: second number
 *
 * Return: The result
 **/

int mod(int a, int b)
{
	return (a % b);
}
