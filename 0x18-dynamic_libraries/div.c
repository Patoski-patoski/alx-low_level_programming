#include "main.h"


/**
 * div- a function to get the modular differences between two numbers
 * @a: first number
 * @b: second number
 *
 * Return: The result
 **/

int div(int a, int b)
{
	return (a / b);
}
